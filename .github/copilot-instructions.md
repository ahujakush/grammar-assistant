# Workspace Notes

- Frontend: React + Vite.
- Backend: FastAPI in `backend/`.
- Core grammar parsing, validation, classification, and transformations must remain deterministic and testable.
- Keep AI explanations supplemental; never use an LLM for mathematical classification.
- Run `npm run build` for the frontend and `python -m pytest backend/tests` for backend checks.
